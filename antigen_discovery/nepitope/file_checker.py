

class MhcPrediction(object):

    def __init__(self, file):

        self.

    def check_consistency(self, iterator):

        headers = list(iterator[0:1])
        if headers[0] == ['', '', '', 'HLA-A0101', '', '', '']:
            if:
                headers[1] == ['Pos', 'Peptide', 'ID', 'nM', 'Rank', 'Core', 'H_Avg_Ranks', 'N_binders']